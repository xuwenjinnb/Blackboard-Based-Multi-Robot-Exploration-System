services:
  redis:
    image: redis:7-alpine
    container_name: inspection-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - inspection-redis-data:/data
    restart: unless-stopped

volumes:
  inspection-redis-data:
